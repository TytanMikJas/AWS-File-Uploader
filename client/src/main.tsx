import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import FileUploadScreen from './view/FileUploadScreen'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FileUploadScreen />
  </React.StrictMode>,
)
